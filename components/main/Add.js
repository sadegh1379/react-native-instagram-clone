import React from 'react';
import { Text , View } from 'react-native'

const Add = (props)=>{
    return(
        <View>
            <Text>Add Screen</Text>
        </View>
    )
}

export default Add;